{
  "name": "dripcord",
  "version": "0.0.1",
  "description": "",
  "main": "dist/app.js",
  "scripts": {
    "clean": "node scripts/clean.js",
    "start": "npm run clean && npm run build && node --no-warnings dist/app.js ",
    "build": "tsc --build",
    "dev": "npm run clean && tsc && node --no-warnings dist/app.js --dev",
    "lint": "npx @biomejs/biome lint --write ./src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MasnyPen/Dripcord.git"
  },
  "author": "MasnyPen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MasnyPen/Dripcord/issues"
  },
  "homepage": "https://github.com/MasnyPen/Dripcord#readme",
  "dependencies": {
    "discord.js": "^14.18.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
